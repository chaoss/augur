CREATE TABLE IF NOT EXISTS augur_data.d0_contributor_engagement (id SERIAL PRIMARY KEY, updated_at TIMESTAMP, username_github TEXT, username_gitlab TEXT, full_name TEXT, country TEXT, social_links JSONB, company TEXT, contributions_last_year INT, has_starred BOOLEAN, has_forked BOOLEAN, is_watching BOOLEAN, collected_at TIMESTAMP, platform TEXT, repo_id BIGINT REFERENCES augur_data.repo(repo_id), UNIQUE(username_github, repo_id));
